"""
Pydantic request/response models shared across routers live here.

Example (added later):

    class ChatRequest(BaseModel):
        message: str

    class ChatResponse(BaseModel):
        reply: str
"""
