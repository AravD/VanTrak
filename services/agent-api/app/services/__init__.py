"""
Business-logic services.

The reusable functions that BOTH the HTTP routers and the agent tools call, so
scheduling/staffing logic lives in exactly one place. Filled in as we port logic
out of the frontend/DB (e.g. capacity calc, rescue logging).
"""
