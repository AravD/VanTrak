"""
Agent tools for outbound notifications (built in the agent phase).

Planned tools:
- send_email(to, subject, body): wraps the existing Supabase `send-email`
  edge function so the agent can notify a driver or manager.
"""
